ADD SERIALIZATION AND VALIDATION FOR NON SQL DB, FOR EXAMPLE NOW SEVERAL SAME EMAILS CAN CONSIST ETC
ADD LOGING TO DOCKER SERVICES
ADD TESTS
ADD LOCUST AND SEED SCRIPT


cursova/
├── services/
│   └── ecommerce/
│       ├── postgres/
│       ├── mongo/
│       ├── neo4j/
│       └── redis/
├── benchmark/
│   ├── seed.py
│   ├── cleanup.py
│   ├── locustfile.py
│   └── seed_data.json  ← generated by seed.py, gitignore this
├── docker-compose.yml
└── pyproject.toml


rewrite seed script to write what he did if error (database not online) happened.


remive unique name requirements etc for ecommerce

Write about the need to distinct Database performance and webserver performance and the need in both because we are making website.


Run direct DB tests.

Tell about optimization vs expenses/needs in a doc. Yeah you can make NF4 db with routers caches and perfect indexes, but do you need it if you get 500 users a day?


ecommerce neo4j startup needs fixing

USE BLACK

GC Pause Time — rate(neo4j_dbms_vm_gc_time_g1_young_generation_total[1m]) — shows if JVM garbage collection is affecting performance under load. Interesting for cursova since it's Java-specific overhead other DBs don't have.